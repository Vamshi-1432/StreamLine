specVersion: "v1beta"
serviceId: "StreamLine"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "StreamLine-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
